# Black Moon デザイン

2026-09-20。黒月・低彩度の金属光・静かな余白を軸に再設計。参考画像からは明暗と輪郭光の考え方だけを抽出し、キャラクターや画像そのものは使用していない。

## 四原則

- 近接: 見出しと説明、デモと操作、OS と導入手順をまとまりとして配置する。
- 整列: 共通のページ幅と左右の基準線を持ち、スマートフォンでは単列へ切り替える。
- 反復: セクション番号、細い罫線、プラチナ色の選択状態、見出し間隔を統一する。
- コントラスト: 黒月の輪郭光と大きな文字、明るい特徴セクション、明確な主 CTA で情報の優先順位を示す。配列のホームキーは意味を保つため青を維持する。

## 実装とフォールバック

- CSS の多層グラデーションと SVG の静的ノイズで月面を構成。画像・動画・WebGL ライブラリの追加はない。
- 対応するポインターでのみ小さな視差を加え、スクロール時は一度だけ短く表示を変化させる。常時ループは使わない。
- Web Animations API と IntersectionObserver は装飾だけに使用。本文は最初から HTML にあり、JavaScript がなくても読める。
- 背景ぼかしと均等改行は `@supports` で段階的に追加。未対応でも不透明背景と通常の改行で成立する。
- 動きを減らす設定に対応。キーボード操作、タブの矢印移動、フォーカス表示、本文へのスキップリンクを提供する。
- 既存の本文・リンク・計測を維持。JavaScript 無効時は全 OS の導入手順を表示する。

## 検証

- `pnpm build` と `git diff --check` 成功。
- ローカル Playwright の Chromium、Firefox、WebKit で 320 / 375 / 390 / 768 / 1024 / 1440 px の横溢れなしを確認。
- レイヤー切替、OS 切替と矢印キー、物理キーと画面上キーによる入力、FAQ、JavaScript 無効時、動きを減らす設定への切替を検証。
- 公開前の HTML と LP の見出し・段落を比較し、空白差を除いて全既存文言の保持を確認。ブログ一覧・比較記事・問い合わせページもスマートフォン幅で確認。
- 外部通信を遮断したローカル検証。フォーム送信、外部解析の受信、実機 Safari / iOS / Android は今回未検証。WebKit の検証は Safari 実機の保証ではない。
- `pnpm check` は既存の型エラーが 7 件残る。変更前 HEAD の隔離検証は 8 件で、Hero の不要な SVG 属性の除去により 1 件減少。API の型宣言、Window の型変換、既存 KeyboardViz の問題は今回の範囲外。

「世界一」「全環境保証」とは評価しない。実機での読みやすさ、表示性能、導入・GitHub 遷移の実測を次の改善基準にする。
