# Black Moon デザイン

2026-09-20。黒月・低彩度の金属光・静かな余白を軸に再設計。参考画像からは明暗と輪郭光の考え方だけを抽出し、キャラクターや画像そのものは使用していない。

## 四原則

- 近接: 見出しと説明、デモと操作、OS と導入手順をまとまりとして配置する。
- 整列: 共通のページ幅と左右の基準線を持ち、スマートフォンでは単列へ切り替える。
- 反復: セクション番号、細い罫線、プラチナ色の選択状態、見出し間隔を統一する。
- コントラスト: 黒月の輪郭光と大きな文字、明るい特徴セクション、明確な主 CTA で情報の優先順位を示す。配列のホームキーは意味を保つため青を維持する。

## 実装とフォールバック

### 文字組み

- 2026-09-20 統一パス: 全ページの見出しは明朝 Medium 500、本文・ナビ・操作・配列・成績は Noto Sans JP。装飾ラベルに混在していた OS 依存の等幅書体と Georgia は除去し、コードだけ等幅を維持。成績は桁幅固定で揃える。
- Noto Sans JP は可変 WOFF2 約 376 KiB を自サイト配信。Google Fonts の外部 CSS・接続待ちを撤去。`font-display: swap` でフォント失敗時も本文を表示する。見出しと合わせたフォント転送量は約 553 KiB（キャッシュ前）。速度向上や CWV 合格を保証する値ではない。
- Noto の配布元は https://github.com/google/fonts/tree/main/ofl/notosansjp 。OFL 同梱。元の `NotoSansJP[wght].ttf` を `pyftsubset --text-file=<srcの文字集合> --unicodes='U+0020-007E,U+3000-30FF,U+FF00-FFEF' --flavor=woff2 --layout-features='*'` で変換。記事追加時は両フォントを再生成する。未収録文字とユーザー入力はシステム書体にフォールバック。
- モバイルのナビ・タブは 11px 以上、ナビと記事フィルターは高さ 44px、主要 CTA は 48px 以上。フォーム入力と練習選択は 16px で、iOS の小文字入力時の拡大を避ける。
- ナビに練習室を追加。ブログ・問い合わせの現在地は `aria-current`、記事フィルターの選択は `aria-pressed` で視覚と読み上げを一致させる。

### 段階表示

- `src/lib/reveal.ts` が見出し、特徴カード、導入ステップ、FAQ、記事カード、記事の節見出しなどを一度だけ表示演出。1000ms、同時に入った同じ親の要素を 100ms 間隔、待ちは最大 300ms。移動は 22px、opacity は 0 から 1。番号・見出し・説明は別々に表示し、本文へぼかしはかけない。
- 本文は最初から HTML に保持。LP のヒーローより下は、対応する JS が動作し、reduced-motion が無効な場合だけ `data-reveal-pending` で opacity:0 にする。スクロール開始後、表示領域へ入ったブロックから opacity:0 → 1 で表示する。ヒーローは隠さない。JS 無効・API 未対応・reduced-motion・印刷では本文を表示。キーボードフォーカスが入るブロックも即表示する。アニメーション API の実行例外時には保留状態を解除する。スクロールを奪わない。
- 入力中の領域は演出対象外。フォーカスが入れば当該演出を解除。動きを減らす設定、タブ非表示、印刷時にも解除。月のアニメーションは別管理し、完全一致してから後光が出て止まる既存動作を維持。
- 本文・title・description・canonical・robots・JSON-LD を変更せず、プリレンダリングも維持する。参考: https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics 。検索順位や AI 回答での引用を保証するものではない。

### 既存の文字組み方針

- ブランド・ヒーロー・主要見出しは「しっぽり明朝 Medium」。太いゴシック一辺倒から、縦横の線の強弱を持つ明朝と操作用ゴシックの組み合わせへ変更。
- 見出しのかなは `palt` で字面を整え、本文には適用しない。「最高効率」を大きく、「の」を小さく組む。文言は保持する。
- スマートフォン専用の文字サイズ・字間・行間を設定。配列の文字、入力結果、ボタン、説明文には読みやすいゴシックを維持する。
- 見出しフォントは約 175 KiB の WOFF2 を自サイト配信し、preload + swap で表示。現行 `src/` の文字集合にサブセット化。新しい記事で未収録の文字を追加した場合はサブセットを更新する。
- 配布元: https://github.com/google/fonts/tree/main/ofl/shipporimincho 。OFL を `public/fonts/shippori-mincho-OFL.txt` に同梱。元の `ShipporiMincho-Medium.ttf` を fonttools の `pyftsubset --text-file=<srcの文字集合> --flavor=woff2 --layout-features='*'` で変換。

### 描画と操作

- CSS の多層グラデーションと SVG の静的ノイズで月面を構成。画像・動画・WebGL ライブラリの追加はない。
- 月はページの load 後1.2秒待って一度だけ動く。三日月から約6.4秒で完全に重なり、移動量0で停止。その後1.6秒で後光だけが現れ、以降は静止する。ループ・ポインター視差・軌道線・周囲の署名・常時発光は使わない。月の直径と本文は変化させない。
- モバイルは見出し上に月全体を表示し、月と本文の間隔を短縮。320 / 375 / 390 / 430 / 640px で月の左右端が画面内、見出し・ヘッダーと非重複であることを検証。
- 動きを減らす設定では自動再生せず三日月を静止表示する。「月を動かす」ボタンは消さず、利用者が明示的に開始できる。再生中は「動きを止める」、完了後は「もう一度見る」に切り替わる。設定変更時は手動許可を解除し、完了した月はそのまま維持する。
- 月が画面外・タブが非表示なら演出を一時停止。表示復帰時はユーザーの停止状態を保って再開を判断し、完了した演出は自動再開しない。本文は一度だけ短く表示を変化させる。
- 月本体だけに、静的SVGフィルム粒子の微細な移動・濃度変化と、最大2.4pxのフォーカスブラー・最大3%のカメラ寄りを加える。文字や操作部はぼかさない。重なり完了時点（6.4秒）でブラー0・拡大率1・粒子位置0へ戻して固定。以降は後光のみが現れる。粒子生成フィルター自体は毎フレーム更新せず、点滅フラッシュや画面全体の揺れは使わない。
- Web Animations API と IntersectionObserver は装飾だけに使用。本文は最初から HTML にあり、JavaScript がなくても読める。
- 背景ぼかしと均等改行は `@supports` で段階的に追加。未対応でも不透明背景と通常の改行で成立する。
- 動きを減らす設定に対応。キーボード操作、タブの矢印移動、フォーカス表示、本文へのスキップリンクを提供する。
- 既存の本文・リンク・計測を維持。JavaScript 無効時は全 OS の導入手順を表示する。

## 検証

- 最新調整: 遮蔽円の初期倍率を .94 → .8 に縮小。雲の不透明度は左 .38 / 右 .32。接触光は輪郭 3px・最大 150px の外光へ拡大し、背景全体に一度だけ広がる `hero-flash` を追加（6.56 秒に opacity .85、7.76 秒に消灯）。黒い月は背景光より前に描画する。閃光・揺れ・月は同じ 8 秒の再生管理に従い、reduced-motion では閃光を抑止する。
- モバイル公開確認: 最新 main のデプロイ成功と本番 HTML の再検証ヘッダーを確認。390px / タッチ設定の Chromium・WebKit で両 WOFF2 の loaded 状態と実際のフェード実行を確認。Chromium の使用グリフも Noto Sans JP / Shippori Mincho のカスタムフォントだった。ただし旧カーブは開始約 0.2 秒で opacity .85 前後に達し、分かりにくかったため、1000ms・opacity .08 開始・穏やかな ease に変更。見出しの番号／タイトル／説明を別々に順次表示する。書体は全テキストを一書体にせず、明朝の見出しとゴシックの本文・操作という役割別の統一を継続。iPhone 実機の利用環境は未確認。
- 最新の演出改訂: 遮蔽円は半径の 94%・横 -85px から始まり、6.4 秒で原寸・中央に一致する。雲は左右の描画を重ね、右側にも広げる。接触光は輪郭 2px と広いブルームで強め、6.56 秒で一度だけピークに達して後光へ収束。画面揺れは月ではなく body 全体へ適用し、6.4〜6.96 秒の 560ms だけ最大横 6px・縦 3px の減衰振動を行う。終了時は transform:none、停止・画面外・非表示でも揺れをリセットし、再開時は月の時刻へ同期する。reduced-motion では画面揺れ・カメラ移動・接触発光を抑止する。以下の過去の「本文を動かさない」方針は、今回ユーザーが明示的に希望した短い画面揺れの区間だけ例外とする。
- 雲と漆黒の対比: 月の背後に、放射状に端を減衰させる低周波 SVG ノイズの灰色雲を配置。独立したループは作らず、既存のカメラ移動と停止に従う。黒い円は `#000000` とし、表面のグラデーション・粒子を除去。フィルム粒子は三日月と同じマスクで明るい部分だけに制限し、完全一致後は消える。本文・操作・検索向け HTML は変更しない。画像・動画・外部描画ライブラリの追加はない。
- カメラの微動: 月全体だけを最大横 4px・縦 2px、緩やかな往復で移動。既存のフォーカス・最大 3% の寄りと同期し、6.4 秒の完全一致で移動・拡大・ぼけをゼロに戻す。本文や操作部は動かさず、ループ・マウス追従・端末傾きの取得は行わない。reduced-motion では手動再生時もカメラ移動とぼけを無効化する。
- 重なりのアクセント: 開始から 6.4 秒で月が完全一致した直後、輪郭に沿う銀青色の光を 280ms で一度だけ強め、7.76 秒までに消す。輪郭の位置・サイズは動かさず、既存の後光へ引き継ぐ。紙吹雪・周回装飾・画面全体のフラッシュは追加しない。追加描画は 1 要素の固定シャドウと opacity のみ。停止・再生・画面外の一時停止は月と同期し、reduced-motion では手動再生時もアクセントを非表示にする。
- 統一パス追加検証: Chromium / Firefox / WebKit で、13 ページすべての main 本文・リンク・title・description・canonical・robots・OG・JSON-LD を変更前 HTML と照合し一致。JavaScript 無効状態で比較した。
- 本文フォントのローカル読み込み、カードの順次遅延、フォーカス時と reduced-motion 切替時の解除、記事フィルター状態、ブログでの IntersectionObserver 未対応時の静的表示を検証。月の正確な重なり・停止・再生、練習全 4 モードと保存、6 幅のレイアウトも再検証した。
- `pnpm build` と `git diff --check` 成功。
- ローカル Playwright の Chromium、Firefox、WebKit で 320 / 375 / 390 / 768 / 1024 / 1440 px の横溢れなしを確認。
- レイヤー切替、OS 切替と矢印キー、物理キーと画面上キーによる入力、FAQ、JavaScript 無効時、動きを減らす設定への切替を検証。
- 公開前の HTML と LP の見出し・段落を比較し、空白差を除いて全既存文言の保持を確認。ブログ一覧・比較記事・問い合わせページもスマートフォン幅で確認。
- 外部通信を遮断したローカル検証。フォーム送信、外部解析の受信、実機 Safari / iOS / Android は今回未検証。WebKit の検証は Safari 実機の保証ではない。
- `pnpm check` は既存の型エラーが 7 件残る。変更前 HEAD の隔離検証は 8 件で、Hero の不要な SVG 属性の除去により 1 件減少。API の型宣言、Window の型変換、既存 KeyboardViz の問題は今回の範囲外。

「世界一」「全環境保証」とは評価しない。実機での読みやすさ、表示性能、導入・GitHub 遷移の実測を次の改善基準にする。
