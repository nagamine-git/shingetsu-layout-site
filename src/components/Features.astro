---
import FeatureDiagram from "./FeatureDiagram.astro";

const features = [
  {
    icon: "⌨️",
    diagram: "keys",
    title: "30キー完結",
    badge: "コンパクト",
    description:
      "かなを 30 キー（英字 3 行）に収め、数字行に手を伸ばさず入力できます。頻出かなをホームポジション周辺に集め、標準 US（ANSI）キーボードだけで動きます。",
    stat: "30",
    statLabel: "キーに収録",
  },
  {
    icon: "⚡",
    diagram: "stroke",
    title: "頻度ベースの打鍵数",
    badge: "前置シフト",
    description:
      "高頻度のかなは 1 打鍵。中頻度は ☆・★ キーに続けて文字を打つ前置シフト（2 打鍵）で入力します。よく使う文字ほど少ない打鍵で打てる設計です。",
    stat: "1",
    statLabel: "打鍵（高頻度文字）",
  },
  {
    icon: "🧩",
    diagram: "layers",
    title: "3段階の打鍵規則",
    badge: "濁点は後置",
    description:
      "単打（高頻度）/ ☆・★ の前置シフト（中頻度）/ 清音のあとに ゛ を打つ後置（濁音・半濁音・小書きの母音）の 3 段構え。濁音の位置を別に覚える必要がありません。Karabiner-Elements（macOS）・Google 日本語入力（Windows）・hazkey（Linux）で導入できます。",
    stat: "3",
    statLabel: "段階の規則",
  },
] as const;
---

<section id="features" class="content-section feature-section">
  <div class="page-width">
    <div class="section-heading" data-reveal>
      <p class="section-index" aria-hidden="true">01 / PHILOSOPHY</p>
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
        なぜ新月配列なのか
      </h2>
      <p class="text-slate-400 max-w-xl mx-auto">
        従来のローマ字入力・JIS かな入力の課題を根本から解決する、
        シンプルな設計思想。
      </p>
    </div>

    <div class="feature-grid">
      {features.map((f) => (
        <div class="feature-card" data-reveal>
          <div class="flex items-start justify-between mb-4">
            <span class="text-xs font-medium px-2 py-0.5 rounded-full bg-brand-500/15 text-brand-400 border border-brand-500/20">
              {f.badge}
            </span>
          </div>

          <FeatureDiagram kind={f.diagram} uniqueId={`feature-${f.diagram}`} />

          <h3 class="text-xl font-bold text-white mb-2">{f.title}</h3>
          <p class="text-slate-400 text-sm leading-relaxed mb-6">{f.description}</p>

          <div class="mt-auto pt-4 border-t border-white/10 flex items-baseline gap-2">
            <span class="text-3xl font-bold text-brand-400">{f.stat}</span>
            <span class="text-sm text-slate-500">{f.statLabel}</span>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
