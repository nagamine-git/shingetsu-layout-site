# 新月タイピング / `/practice`

新月配列の習得から高速化までを、独立した1ページにまとめる。LPの20文字デモは保持する。登録不要・無料・Astroとvanilla TypeScriptのみ。追加の実行時ライブラリ、iframe、外部の問題文APIは使わない。

## 目的と限界

「医学的に最速」「世界最高の学習効果」を実証した製品ではない。研究を設計の参考にした仮説実装であり、新月配列における比較試験は未実施。短期の自己ベストだけでなく、日を空けた保持・異なる文脈への転移を重視する。身体症状を診断する機能はない。

主たる評価候補は、同条件・ヒントなし・正確率95%以上での入力速度と、それを得るための練習時間。将来の比較試験では、事前の経験、入力装置、初期能力、教材への既知性を揃え、翌日・1週間後の保持と未練習文の成績を別に評価する。固定文の反復だけで「一般的な実力の向上」とは判定しない。

## 先行サービスとコード調査

調査日：2026-09-20。設計上の着想を参考にし、他サービスのコード・問題データ・画像は転載していない。

| 参照先 | 調査した実装・振る舞い | 今回の採用と差分 |
| --- | --- | --- |
| [keybr](https://www.keybr.com/) / ローカルフォーク `../keybr.com`、`8dd2019a7e95fe93af543ba9b6f417dfacb02f44` | `packages/keybr-lesson/lib/guided.ts` の対象文字拡張・最弱文字のフォーカス、`key.ts` / `target.ts` の信頼度、`learningsession.ts` のセッション区切り、`packages/keybr-result/lib/keystats.ts` の文字別時系列、`packages/keybr-keyboard/lib/layout/ja_shingetsu.ts` の配列・接頭辞・後置修飾 | 少数文字からの段階学習、個人別の弱点選択、ガイドの削減。音韻擬似語生成は採用せず、実在する語と書き下ろし短文だけを選ぶ。keybrの信頼度モデルの移植ではない。AGPL-3.0のコードはコピーしない。 |
| [Monkeytype](https://github.com/monkeytypegame/monkeytype/tree/91bd24bb8513785c7364cbea29296ff7adafac41)、`91bd24bb8513785c7364cbea29296ff7adafac41` | `frontend/src/ts/test/practise-words.ts` のミス語・遅い語・前語との組の復習、`test/events/stats.ts` のイベントと計測区間・累積速度、`utils/numbers.ts` の5文字換算WPM、時間制・ガイドなしの集中UI | 30/60/120秒、ミスと遅い2打鍵から別の文へ戻る導線、速度グラフ。日本語かなは5文字で1語とはせず、かな/分。誤打で進まない方式なのでMonkeytypeと速度を直接比較しない。GPL-3.0のコードはコピーしない。 |
| [ATC tester](https://o24.works/atc/) / 公開 `script.js?5b` | 初回入力で開始、compositionendとinputの区別、確定文字列の一致する先頭部分、変換後文字の1分計測 | IME実践モード。確定前の変換は採点せず、確定後に照合する。公開スクリプトを閲覧したが、ライセンス不明のコードや課題文は転載しない。 |
| [e-typing](https://www.e-typing.ne.jp/) | テーマを持つ日本語の題材、かな/ローマ字の分離、腕試しから反復する導線 | 日常・仕事・連絡などの題材、意味と読みの併記。非公開エンジンを取得・解析したという意味ではない。 |
| [寿司打](https://sushida.net/) | 短い時間制の挑戦、即時結果、複数の正しい入力経路、表示ガイド切替 | 短いセッションと明確な終了、別打鍵の許容。食材・UI・問題文・WebGL成果物を流用せず、焦らせる演出や音は採用しない。 |

### 研究・安全性

- [Keith & Ericsson, 2007](https://pubmed.ncbi.nlm.nih.gov/17924799/)：日常的タイピングと向上を意図した練習を区別する研究。弱点を指定して練習する考え方の参考。新月配列の介入試験ではない。
- [Dhakal et al., CHI 2018](https://userinterfaces.aalto.fi/136Mkeystrokes/)：大規模な観察データから打鍵・誤打・連続キーを分析。2打鍵の集計の参考であり、観察された相関を因果効果や一律の指使いの優越性と解釈しない。
- [Buch et al., 2021](https://pmc.ncbi.nlm.nih.gov/articles/PMC8259719/)：短い運動系列課題と休息中の再生。一方、[2025年の反証研究](https://pubmed.ncbi.nlm.nih.gov/41150724/)は休憩直後の改善が持続的学習を示すという解釈に疑義を示す。休憩による学習倍率や最適秒数をうたわない。
- [OSHA Keyboard Workstation Guidelines](https://www.osha.gov/etools/computer-workstations/components/keyboards)：自然な手首・肩の姿勢、短い休止を参考にする。痛み・しびれがあれば中止。継続する症状は専門家への相談を促す。医療・リハビリの代替にはしない。

- [Shea & Morgan, 1979](https://gwern.net/doc/psychology/spaced-repetition/1979-shea.pdf)、[高文脈干渉のメタ分析（Scientific Reports, 2024）](https://www.nature.com/articles/s41598-024-65753-3)：課題を混ぜて練習するほうが練習中の成績は下がっても保持・転移に有利という文脈干渉効果。弱点ドリルで弱点を含む文7割・含まない文3割を混ぜる根拠。かな入力への直接の追試は未確認で、比率は仮説。
- 疾走（オーバースピード）：快適速度を超える短時間練習で上限を押し上げる方法は競技タイピングの実務知見にとどまり、査読研究は 2026-09-21 の調査で確認できなかった。効果量・副作用（誤打の癖の固定）は未検証のため、正確率90%未満で目標を下げる助言を出し、15秒の記録は他の秒数の自己ベストから分離する。

20秒の休憩提案、1/3/7/14日の間隔、95%基準、1.5秒/打鍵、80%での段階移行、3%の目標上乗せ、弱点ドリルの1.4倍・1.5倍・7:3、疾走の+8%・90%は**製品上のヒューリスティック**。研究がそれらの具体値を最適と実証したわけではない。休憩はスキップでき、段階・ガイドも手動で選べる。

## 機能

| モード | 課題と終了 | 特徴 |
| --- | --- | --- |
| 習得 | 段階内の語・短文、概ね36/75文字（最大14題） | 6段階。未経験・誤打・遅さ・復習期限で選択。14題制限により文字数は前後する。 |
| 復習 | 選んだ段階までの語・短文 | 習得と同じ適応選択を使う。結果のボタンから文字または2打鍵を明示的に重点化できる。 |
| 高速化 | 語句・短文／まとまった文章、30/60/120秒 | 正確さを保つ速度練習。複数文が自動で続く。原則ガイドなし。 |
| 定点測定 | 学習プールから分離した固定8文、60秒 | ガイドなし。同じ文の同じ順。記録比較用v1。習熟モデルの更新には使わない。反復すれば既知になるため、初見テストとは呼ばない。 |
| IME実践 | 同じ固定8文、60秒 | OS側の新月配列と日本語IMEを使用し、漢字・句読点を含め照合。完全文が一致すると次の文へ。どの配列かは判定できない。 |
| 集中 | 語句・短文／まとまった文章、30/60/120秒（初期60秒） | 課題文・小さな残り時間・最低限の操作だけ。終了後に速度・正確率・次の改善点。Enterまたはタップで同条件の新しい課題へ。自動再開しない。 |

### 集中モード

`/practice/?mode=focus` から直接開くか、モード選択の「集中」から入る。サイトのヘッダー、履歴、習熟マップ、ライブ成績、グラフを隠す。打鍵ガイドは初期非表示で、ヒントや設定から戻せる。タップ入力のキーボードは隠さない。「モード選択」で通常画面に戻り、履歴や詳しい解説を確認できる。

最初の打鍵から計測し、期限で必ずリザルトへ。結果の表示中は計測せず、自分のタイミングで休める。通常モードの20秒休憩カウントは挟まず、結果画面のEnterまたは「もう一度」で新しい課題へ進む。Escは練習中・結果画面から同じ課題列の先頭へ即リトライし、入力・時間をリセット。次の打鍵までは計測しない。入力済みの途中ランは中断記録として一度だけ保存し、自己ベストに含めない。未入力ランや結果画面からのリトライで記録を追加しない。リトライ時に中断リザルトへのフォーカス・スクロールを挟まない。

ショートカットは長押し・変換中・修飾キーを除外し、設定のselect等のキー操作を奪わない。「中断する」、フォーカス離脱、モード選択では再開せず中断記録として終了。習得・復習・その他の時間制モードのEsc操作は変更しない。終了前にページを閉じた未完了ランの自動保存は行わない。

出題・計測は高速化と同じ方式だが、モードごとに記録を分離する。秒数・入力方法・ガイド・文章の種類はリトライで維持。練習時間の最適性や、休憩による学習効果は保証しない。

文章は `src/data/trainingCorpus.ts` に書き下ろし196題＋測定8題。全学習段階に、その段階の全対象文字を含む教材がある。漢字表記は意味の提示用、シミュレーターで入力するのは読み。直近30題には出題ペナルティをかける。同じセッション内では同一問題を重複選択しない。時間制で用意した文を使い切った場合は次の一組を継ぎ足す。

### まとまった文章

集中・高速化の設定「練習する文章」で選択。初期設定の語句・短文は残す。`/practice/?mode=focus&material=paragraph` は文章練習への直接リンク。`src/data/trainingParagraphs.ts` に、4文で一つの文脈を持つ書き下ろし16本を収録する。日々の風景、道案内、会議準備、引き継ぎなど。外部作品・サービスの文章は転載しない。

段落の原文を全文表示し、読みの欄は約3行の高さに収める。現在の入力位置が外れたときだけ欄内をスクロールし、ページ全体は動かさない。文末でランを分割せず、段落全体を連続した入力として扱う。時間が来れば段落の途中でも終了し、完了済みのかなだけを計上。未完了の複数打鍵かなは文字数に含めない。Escは同じ段落列の先頭に戻り、Enterは次の出題へ。

段落用プールから概ね900かな分を選び、不足時は次の一組を継ぎ足す。完了しなかった現在の段落も直近教材として保存し、再出題の重みを下げる。履歴の`material`と比較キーで短文／文章を分離し、既存の比較キー・成績は維持する。旧バックアップの`material`省略は短文として読む。学習・復習・固定測定・IMEの教材や出題順序は変えない。

### 入力エンジン

- 権威データは既存 `src/data/romantable.json`。別フォークの配列版や独自キー定義を混ぜない。公式元ファイル `../shingetsu-layout/shingetsu-romantable.txt` と照合する。
- 全読みをローマ字テーブルのかな単位へ分割し、各単位の直接経路と分解経路を列挙する。例えば `きゃ` は `;bk` / `;kb`、濁音・拗音は公式テーブルで成立する別経路を許容。ガイドは最短経路を一つ示すが、残りの候補も採点する。
- IMEを再現する自由変換エンジンではない。正しいキーだけが進行し、誤打は残したまま次の正しい入力を待つ。Backspaceによる誤字削除は不要。
- PCは `KeyboardEvent.code` の物理位置。IME・OSのリマップはオフの英数QWERTY。長押し、Ctrl/Meta/Altのショートカット、変換中を除外し、Tabは通常のフォーカス移動を維持する。
- タップはPCと別プロファイル。スマートフォンの配置学習であり、物理キーボードでの運動学習と同じとは説明しない。
- D/K等の入力途中はキー凡例が現在の接頭辞に応じて切り替わる。ガイドに慣例的な指位置を表示するが、全員に特定の指使いを強制しない。
- IME実践はcomposition中を除外し、確定後のNFC文字列を照合。Backspace・再変換で修正できる。貼り付け・ドロップは通常のイベントを抑止するが、予測変換・スクリプトの完全検出や競技用不正防止は提供しない。

### 適応と保持

各かな／拗音に練習回数、自力正答・誤打の直近20試行、成功時の直近20打鍵平均時間、前回時刻、次回期限を持つ。拗音単位の観測は構成かなにも共有するため、個々のかなを独立実験で測った値ではない。

- 同じかなを3回経験したら自動ガイドを隠す。1.8秒迷うか、誤打すると戻る。常時表示／非表示も選択できる。
- ガイドつき正答を自力試行の成功に数えない。誤打はガイドが戻った場合でも失敗に数える。
- 安定目安：自力試行・誤打の記録が6回以上、直近正答率95%以上、ガイドなし成功の時間標本が3つ以上、打鍵あたり中央値1.5秒以内。段階の80%以上が安定したら次へ。足りない文字は復習で扱う。
- 出題重みは未経験、失敗率、時間、期限切れを反映。全体を極端に苦手だけに狭めず、既知語・文字を混ぜる。結果からの指定対象を含む文章には追加の重み。
- 一度のセッション中に繰り返しただけでは復習間隔は伸びない。期限が来た後の自力成功で1→3→7→14日。誤打・ガイド頼りの観測は10分後に再確認。スケジュールは忘却率を推定した医学的モデルではない。
- 未完了の文字でも誤打があれば弱点に保存。未入力や長押しだけで習得扱いにはしない。

## 計測

### シミュレーター

- かな/分：完了した読みのコードポイント数÷分。拗音は構成かな数、句読点は各1。途中の未完了打鍵列は文字数に含めない。英語WPMではない。
- 打鍵正確率：正打鍵÷採点対象の全打鍵。誤打の時間も分母の時間に含まれる。
- 最初の打鍵で開始。習得・復習は明示停止やフォーカス離脱で止まり、再開操作まで時間を除外。時間制は離脱・中断で参考記録として終了し、自己ベストに含めない。
- 速度は1秒未満なら表示せず、保存値は0。期限はperformanceの実経過で判定し、遅れたタイマーの回数には依存しない。
- 初回かな／再開後の最初のかなには、準備時間が計れないため速度標本を付けない。キー間時間は正しい連続入力のみ、0超〜10秒。誤打と停止の境界は跨がない。
- 2打鍵は同じ入力方法の累積、指数更新係数0.2、4標本以上から表示。遅いと判定する医学的閾値ではなく相対順位。
- リズムは正しいキー間隔の変動係数から `max(0,100×(1−CV))`、10標本以上のみ。独自の参考指標。グラフは各秒の累積平均かな/分。

### IME実践

速度は完了した文＋現在の文の一致する先頭文字数÷分。間違っている位置より後の文字は先に加算しない。未確定変換は除外。一致率は現在までの確定入力に対する一致する先頭部分で、修正後の値。打鍵正確率や生の誤打率を装わない。シミュレーターのかな/分とは別の単位・履歴として表示する。

### 比較の条件

同じモード・入力方法・時間設定・段階・文章の種類・バージョンの記録だけを候補とする。ガイド使用、中断、95%未満、15秒未満、10文字未満を自己ベスト／目標ペースから除外。高速化のペース目安は比較可能な記録が3つ以上ある場合、直近5つの中央値に3%を加えたもの。上達率を保証する値ではない。

## 保存とプライバシー

- `localStorage["shingetsu-training-v1"]` のみを操作。既存LPの `shingetsu-practice-v1` と独立。
- PC／タップのかな・2打鍵集計、直近180結果、直近30問題ID。IMEの入力文そのものは保存しない。
- 文字列や打鍵ログを外部送信するコードは追加しない。サイト共通のアクセス解析は残ることを明記。練習イベントや成績のAnalytics送信は追加しない。
- 保存不可でも練習でき、告知する。JSON書き出しと読込み、削除（確認つき）。読込みは600KBまで、バージョン・ホワイトリスト・数値上限・配列件数を検証。読み込んだテキストをHTMLとして挿入しない。
- バックアップには行動履歴や文字ごとの集計が含まれる。共有は本人の判断で行う。自動同期はない。

## 実装配置

- `src/pages/practice.astro`：専用SEO、canonical、WebApplicationの構造化データ。
- `src/components/TrainingLab.astro`：意味のあるSSR本文、操作UI、計測定義、研究の限界、参考文献。
- `src/data/trainingCorpus.ts`：教材、段階、測定用分離プール。
- `src/data/trainingParagraphs.ts`：文脈のつながる書き下ろし段落16本。
- `src/lib/training.ts`：入力経路・タイマー・計測・出題・習熟・永続化検証。DOM非依存。
- `src/lib/trainingClient.ts`：入力・composition・フォーカス・表示・保存I/O。
- `src/styles/global.css`：`.lab-*` / `.training-page` に限定。モバイルは練習画面を設定より先に出す。

## キーボードの表示形状

- 全キーボード表示で、一般的な段ずれ（0 / 0.25 / 0.75キー幅）、格子、左右5列ずつの分割を選択できる。キー順・打鍵経路・採点は変更しない。
- 共通コンポーネント `KeyboardGeometry.astro` が同一ページの選択とSVGを同期。DOM配列は共通 `.keyboard-row` で表示する。ヒーロー、レイヤー図、体験、20文字練習、本格トレーニング、指割り当て図が対象。
- `shingetsu-keyboard-geometry-v1` に形状だけを保存し、ページ移動・再読込・別タブに反映。保存不可でも切替でき、不正値は一般配列に戻す。成績データ・バックアップとは独立。
- 集中モードも設定から変更可能。本格トレーニングのセッション中は設定とともにロックする。IMEやガイド非表示ではキーボードを無理に表示しない。JavaScript無効時も静的な配列図は一般配列で表示し、操作できない選択欄は隠す。

## 長音の入力互換

- PCシミュレータでは「ー」tokenに限り、D→Pと単打のハイフン系入力（`- － − ‐ ‑ ‒ – — ― ﹣ ｰ ー`）を許可する。公式テーブル・`encodings()`・先頭の打鍵ガイドはD→Pのまま。
- 単打は1試行・1打鍵で記録する。D入力済みのハイフンは誤打としてDを保持し、Pで完了。非長音の文字・漢字「一」・波ダッシュ・下線・不可視のソフトハイフンは代用しない。
- IME実践はお題が「ー」の位置だけ同等と扱う。NFC正規化後の比較に限定し、表示本文・入力欄を置換しない。composition中は採点せず、全文一致と長さ一致で完了する。
- PC・IMEの比較signatureに`:longmark-v1`を付け、旧記録とPB／ペースを分離する。旧履歴・保存スキーマは維持。画面タップは操作経路が変わらないため比較条件も維持する。
- ハイフン単打でもかな数・試行数・時間・長音の習熟集計には含めるが、正規キー列でないハイフン前後は2打鍵分析から除外する。架空のD/P打鍵やハイフンを飛ばしたペアを作らない。正しい打鍵間隔のリズム集計は維持する。

## 音と成長の表示

- 「音のアトリエ」から、独自のWeb Audio合成BGM（黒月・静月・月行）と打鍵音（FELT・TACTILE・GLASS）をそれぞれ3種類選べる。録音済みの管弦楽曲や既存作品の音源ではない。外部音源の取得・送信は行わない。
- 再読込時は必ず消音。明示的な有効化・試聴操作の後だけ音を出す。BGMとSEは独立した音量、設定は練習記録とは別に端末内保存。3秒の試聴、練習中の消音、停止時のBGM停止に対応する。Escのやり直しでは曲の進行を初期化しない。
- SEはシミュレータで採点した打鍵につき1回。IMEは変換中に鳴らさず、確定入力の更新につき1回。音は入力処理の成功条件ではなく、未対応・再生失敗時も無音で継続できる。
- セッションに開始・終了時の音設定、有効状態・再生失敗・途中変更を保存する。旧バックアップに音記録がなければ「不明」と扱い、無音だったとは推定しない。音条件は速度の比較署名を分割しないため、グラフ・結果に混在を明記する。音による技能向上や因果効果は主張しない。
- 成長グラフは入力方法と比較署名（モード・秒数・段階・文章種別・入力規則）が一致する保存済み記録を、ローカル日付の7日・30日・全件で選択。縦軸は速度と正確率を分離し、横軸は日時。全記録を表示し、比較対象外は菱形にする。選択期間内に比較可能な5回が揃った地点から、その直近5回の速度中央値を表示する。グラフと同じ数値を表でも読める。
- 今週の目標は2・3・5・7日から選択し、月曜開始で入力方法別に集計。15秒以上・10文字以上・中断なしのセッションを数える。失敗への罰則や連続日数のリセットは設けない。
- 「しるし」は練習完了・正確な練習・別日の再現・文章・復習の5つ。保存中の直近180件から都度計算するもので、永久保存の称号ではない。新しいしるしや自己ベストを得た結果だけ、短い光と音で知らせる。動きを減らす設定では光のアニメーションを止める。
- 今日の課題は復習期限の文字数を示し、既存の適応復習へ接続する。XPのための連打やランキングは導入しない。習熟マップ・成長グラフは通常画面、集中モードでは結果の差分と新しいしるしだけを表示する。

## 検証

新規のテスト基盤はリポジトリに導入せず、一時スクリプトで検証する。`pnpm build` と `pnpm check` を実行。型チェックは既存の6件（Subscribe、contact、cloudflare型）のエラーを区別し、本変更に新規エラーを残さない。

- 全204教材×3経路選択＝612ラン。全かな対応、全段階の対象文字カバー、意味と読みの表記を確認。
- 追加の文章16本×3経路＝48ラン。各4文・129〜156かな。3ブラウザで入力位置の追従・欄内のキーボード操作・途中かなの採点・段落の切替・Esc/Enter・短文との成績分離・旧データの読込みを検証する。
- 別経路、濁点、半濁点、拗音、誤打、長押し、停止・再開、計時境界、習熟・期限、自力とヒントの区別、入力方法の分離、壊れた保存値。
- Playwright Chromium / Firefox / WebKit。320・390・768・1024・1440px。完走、各モード、時間制、IMEの合成イベント、JSON復元、フォーカス、JS無効、ストレージ無効、ページエラー、横溢れ。
- 全3形状×5画面幅×3ブラウザで各配列図の段ずれ・中央の分割・キー順不変・横溢れを検証。全6モードの選択欄、同一ページ・ページ間・別タブの同期、再読込、不正値・保存不可・JS無効時、セッション中のロック、タップ入力も確認する。
- ブラウザのIMEイベントは合成による検証。実機のOSリマップ、日本語IME全製品、iOS/Android実機、スクリーンリーダーの読み上げは別途実機検証が必要。
- axe-core 4.13.0のWCAG 2.2 AA自動検査を5モードで実行。自動検査は手動アクセシビリティ評価の代替ではない。
- 集中モードは追加で3ブラウザ・5画面幅、30/60/120秒の期限、初打鍵前の待機、結果での待機、Enter再試行、修飾キー・長押し・変換中の除外、設定の保持、途中離脱、タップ入力、保存と復元を検証。開始前・練習中・結果の3状態もaxeで検査する。
- デプロイ後にカスタムドメインのHTTP・新しいHTMLとJS・モバイル表示・開始・保存まで検証する。デプロイジョブの成功だけで確認完了とはしない。
- 音・成長機能は一時スクリプトで、3ブラウザ×5画面幅、3種類ずつの選択、明示的有効化・消音・再読込、集中30秒・Esc/Enter、成績への音設定保存、グラフ・週目標・復習導線を検証。Chromium/Firefoxは音源ノード生成、WebKitは検証環境の音声デバイス開始エラー時に無音で継続できることを確認する。実機Safari/iOSでの発音と、音色の聴感品質は別途確認が必要。
- 音エンジンの遅延resumeと消音の競合、非表示化、再開時の曲進行保持、タイマー遅延時の過剰発音回避、ストレージ拒否はモック検証も行う。成長は4/5回境界・条件分離・日付/週境界・180件制限・旧記録/音データの復元を検証する。
- 音設定UIのaxe検査は違反なし。結果画面には既存の `lab-pair-list` 空状態（ul直下のテキスト）のlist違反が残る。本変更では既存の型エラー6件、ESLint未導入も修正対象にしない。

## 「練習する」と「測る」の 2 ボタン（2026-09-21）

利用者にモードを選ばせない。学習者は自己調整が苦手で、ブロック練習や再読を「効いた」と錯覚しやすい（[Kornell & Bjork, 2008, Psychological Science](https://doi.org/10.1111/j.1467-9280.2008.02127.x)）ため、記録から次の 1 ブロックをシステムが決め、理由を一文で示す。訓練と測定は分ける（固定文の「測る」）。IME は入力方法（PC / タップ / IME）に移し、集中は画面レイアウト扱い。手動選択は「メニューを自分で選ぶ」に畳んで残す（透明性・テスト容易性）。

`src/lib/trainingPlan.ts` の `planSession(profile, method, now)`：

1. 段階が残る人：復習期限が 3 文字以上で直前が復習でなければ **復習**、それ以外は **習得**（提案段階、ヒント auto）
2. 全段階に入った人：復習期限 ≥ 1（直前が復習でない）→ **復習**（ヒントなし想起）
3. `weakTargets` ≥ 1（直前がドリルでない）→ **弱点ドリル 45 秒**
4. 直前が比較可能な 60 秒で正確率 ≥ 96%、疾走目標あり → **疾走 15 秒**
5. 直前が疾走 → **定着 60 秒**（疾走の正確率 < 90% なら「正確さを先に」と助言）
6. それ以外 → **高速化 60 秒**

停滞判定は使わない（データ量に依存せず頑健な交互）。保存形式・signature は変更なし。1 ブロックごとに再計画し、セット編成は未実装。

## 熟達後の伸び悩み対策（2026-09-21 追加）

| 機能 | 何をするか | 実装 | 検証 |
| --- | --- | --- | --- |
| 弱点ドリル（モード 07） | 本人の中央値を基準に、遅い・不正確なかなと 2 打鍵を最大 6 件自動選定。それを含む文 7 割・含まない文 3 割で 45 秒。結果で弱点ごとに基準値→この回の中央値を表示 | `src/lib/trainingDrill.ts`（`weakTargets` / `selectDrillPassages` / `drillChanges`）、UI は `trainingClient.ts` | vitest + E2E |
| 疾走（高速化モードの 15 秒） | 30〜120 秒の比較可能な自己ベスト +8% を目標に表示。結果で「速すぎ／上限更新／もう一本」の助言。15 秒の記録は同条件どうしでのみ比較 | `sprintTarget` / `sprintAdvice` | vitest + E2E |
| 指の速度マップ | 各キーへ入る 2 打鍵の平均間隔をキーボード上に本人比で濃淡表示 | `keyHeat` | vitest + E2E |
| 今日の小さな課題 | 復習期限がなく全段階が安定している人には、復習の代わりに弱点ドリルを勧める | `trainingProgressView.ts` | 手動 |

弱点は絶対値ではなく本人比で選ぶため、上級者でも常に「次の一手」が出る。一方で、記録が少ない初学者には弱点が選べず通常の短文練習になる。

## テスト

- 単体（`pnpm test`、vitest）：`src/lib/__tests__/`。採点・保存・弱点選定・目標計算の純粋関数。CI（deploy.yml）でビルド前に実行。
- E2E スモーク（`pnpm test:e2e`、playwright-core + ローカル Chromium）：トップの月描画、弱点ドリルの選定→打鍵→中断→結果保存、疾走の目標表示、速度マップ。CI では実行しない（ブラウザ依存を避ける）。マージ前にローカルで実行する。
- 手動：3 エンジン・モバイル幅の見た目、音、IME 実践は自動化していない。
