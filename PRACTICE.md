# 新月の練習室

LP の `#practice` に埋め込んだ、インストール不要の打鍵順トレーニング。従来の自由入力デモは残す。React、iframe、外部サービスへのログインは使わない。

## 参考と実装の境界

ローカルの以下を調査した。

- `../keybr.com`、commit `8dd2019a`。`packages/keybr-keyboard/lib/layout/ja_shingetsu.ts` の新月配列対応。
- `../keybr.com-worktrees/shingetsu-steno`、commit `fc861625`。`packages/keybr-lesson/lib/guided.ts` の学習対象の拡張と苦手文字へのフォーカス。
- `packages/page-practice/lib/practice/state/lesson-state.ts` のレッスンと結果の分離。

Keybr フォークのライセンスは AGPL-3.0。今回はソース・UI・問題データをコピーせず、「少数文字から練習」「ミスに基づく重点復習」という考え方を参考に独立実装した。Keybr と同じ習熟度モデル・機能一式を実装したものではない。

## 練習仕様

- 1レッスン20文字。基本8文字、前置シフト、濁点・半濁点、苦手復習の4コース。
- `src/data/romantable.json` を逆引きし、文字ごとの最短打鍵列をガイドする。等長なら固定順。手書きの別配列を持たない。
- フォークや別バージョンの配列を混ぜず、現在のサイトの表に準拠する。
- 正しいキーでのみ進む。誤打はミスとして加算し、次に必要なキーを文字と色で示す。長押しリピート・IME変換中・ショートカットは採点しない。
- 指定順を覚える練習であり、IMEの変換・確定・削除を再現する自由入力ではない。画面内にも明記する。
- 復習では各文字の打鍵ミス率に応じて出題の重みを増やす。未練習文字にも重みを付け、同じ文字を連続出題しない。速度による自動解放や文章生成は未実装。
- PC は英数入力で、`KeyboardEvent.code` による物理位置。画面タップは独立した入力方法として選択する。記録を混ぜない。

## 計測と保存

- 打鍵正確率 = 正打鍵 / 全採点打鍵。濁点等の途中のキーも1打鍵。
- かな/分 = 完了かな数 / アクティブ入力時間（分）。WPM と呼ばない。最初の打鍵から最後の打鍵まで計測し、入力枠のフォーカス離脱・ウィンドウ離脱・バックグラウンド中は停止。次の打鍵で再開する。
- 1秒未満は速度を表示せず、保存値は0。これは速度が評価できない短時間のセッションであり、最速記録として扱わない。
- 完了時のみ、入力方法別の文字ごとの打鍵数・ミス数と、直近20件のレッスン結果を localStorage `shingetsu-practice-v1` に保存。
- 破損・不正なデータは検証して破棄。保存不可でも練習可能。リセットは確認後、このキーだけを削除する。
- 既存の利用計測には `practice_start` / `practice_complete` と `mode` / `input_method` のみ追加。入力文字列・文字別成績・正確率・速度は送らない。実イベント受信・集計は別途確認が必要。

## 検証と今後

一時検証スクリプトでレッスン生成、全コースの打鍵列、濁点・半濁点、ミス、停止時間、履歴検証、苦手の出題重みを確認。Playwright の Chromium / Firefox / WebKit で全4コース完了、PCとタップの分離、IMEガード、再読み込み、削除、破損・利用不可ストレージ、320〜1440pxを検証。既存デモと原文保持も回帰確認する。実機の日本語IME・OS側のキーリマップ・iOS/Androidは未検証。

次の候補は、配列バージョンの整合を確認したうえでの短文コース、文字別の所要時間、実績に基づく段階解放。まず開始→20文字完了の到達状況を観測し、初回離脱を減らす。数値データなしに学習効果や継続率の向上を断定しない。
