# 任意ログイン・記録同期の設計案

現時点では未実装。認証サービス・同期用データベースは未接続であり、使えないログインボタンは公開しない。

## 目的と範囲

- 練習は従来どおり登録不要。ログインは端末間で記録と設定を引き継ぎたい人だけに提供する。
- 初期範囲はログイン、ログアウト、記録・設定の同期、データ書出し、アカウント・同期データ削除。公開プロフィール・ランキングは別途判断する。
- サイトの説明・教材・SEO用本文は認証の外に置く。ログインが失敗してもローカル練習を妨げない。

## 認証候補

パスワードをサイト側で管理せず、管理型認証を使う。候補は Supabase Auth のメール認証と、必要に応じて Google ログイン。採用・プロジェクト作成は未決定。

- [メールのワンタイムコード／マジックリンク](https://supabase.com/docs/guides/auth/auth-email-passwordless)
- [Google ログイン設定](https://supabase.com/docs/guides/auth/social-login/auth-google)
- [認証と行レベルアクセス制御](https://supabase.com/docs/guides/auth)

## 既存記録を壊さない同期

- 初回ログイン時に、この端末の既存記録を取り込むか明示的に選んでもらう。ログインだけで自動送信しない。
- 現行の累積プロフィールJSONを端末間で上書き・単純加算しない。セッションIDを持つ記録と同期バージョンを用意し、再送・同時編集による重複計上を防ぐ。
- 古い累積データには完全なイベント履歴がないため、一度限りの移行元スナップショットとして管理する。移行前バックアップを保持し、別端末から同じバックアップを取り込んでも二重加算しない設計が必要。
- オフライン中もローカル保存し、オンライン復帰時に再送する。同期時刻・未同期・失敗を明示する。練習中は現在の課題や採点状態を置き換えない。
- 形状などの設定と成績は別管理。ログアウト後、共用端末に記録を残すか選択できるようにする。別アカウントのデータを混ぜない。

## 公開前の必須条件

- 認証・DBプロジェクト、送信メールの設定、Google利用時のOAuthクライアント、本番用の厳密なリダイレクトURL設定。
- サーバーで本人確認し、DBでも本人の行だけを読み書き可能にする。別ユーザーIDの指定・未認証・期限切れ・再送・同時更新をテストする。
- セッション失効、CSRF／リダイレクト悪用対策、認証試行のレート制限、キャッシュ禁止、秘密情報のサーバー側管理。
- 保存項目・目的・削除方法をユーザーに説明し、プライバシー表示を更新する。入力文・生の打鍵ログは同期対象にしない。
- 記録の競合・再送・削除後のオフライン端末復帰、ログアウト／アカウント切替、端末変更での実認証を検証してから公開する。
